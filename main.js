var options = {
    strings: ["Kamlesh Kumar", "Web Developer", "Programmer Analyst Trainee in Cognizant"],
    typeSpeed: 40,
    backSpeed: 40,
    loop: true
};

var typed = new Typed(".title", options);